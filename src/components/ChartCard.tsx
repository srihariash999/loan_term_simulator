import { useState, type ReactNode } from 'react'
import { Maximize2, X } from 'lucide-react'
import { Card } from './ui'
export function ChartCard({ title, children, action }: { title: string; children: ReactNode; action?: ReactNode }) {
  const [open, setOpen] = useState(false)
  return <><Card className="p-5"><div className="mb-5 flex items-center justify-between gap-3"><h3 className="font-semibold text-slate-800 dark:text-white">{title}</h3><div className="flex items-center gap-2">{action}<button onClick={() => setOpen(true)} aria-label={`Maximize ${title}`} title="View larger" className="grid h-8 w-8 place-items-center rounded-lg border border-slate-200 text-slate-500 transition hover:border-mint hover:bg-mint/10 hover:text-mint dark:border-white/10 dark:text-slate-300"><Maximize2 size={15}/></button></div></div>{children}</Card>{open && <div role="dialog" aria-modal="true" aria-label={`${title} expanded`} className="fixed inset-0 z-50 grid place-items-center bg-slate-950/70 p-4 backdrop-blur-sm" onMouseDown={() => setOpen(false)}><div className="chart-modal max-h-[92vh] w-full max-w-6xl overflow-auto rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl dark:border-white/10 dark:bg-slate-900" onMouseDown={e => e.stopPropagation()}><div className="mb-4 flex items-center justify-between"><div><h2 className="text-lg font-bold text-slate-800 dark:text-white">{title}</h2><p className="text-xs text-slate-500">Expanded chart view</p></div><button onClick={() => setOpen(false)} aria-label="Close chart" className="grid h-9 w-9 place-items-center rounded-lg border border-slate-200 text-slate-500 hover:bg-slate-100 dark:border-white/10 dark:text-slate-300 dark:hover:bg-white/10"><X size={18}/></button></div><div className="min-h-[480px]">{children}</div></div></div>}</>
}
